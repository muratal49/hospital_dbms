# Copy this file to `.env` in TASK B and set your DB credentials there (do NOT commit .env)
# Example values (replace with your real values):
DB_HOST=localhost
DB_USER=mal
DB_PASS=your_db_password_here
DB_NAME=mal_1
